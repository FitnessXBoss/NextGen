<UserControl x:Class="NextGen.src.UI.Views.UserControls.MessageDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <StackPanel Margin="20">
        <TextBlock Text="{Binding Message}" FontSize="16" TextWrapping="Wrap" Margin="0,0,0,20"/>
        <Button Content="OK" Command="{Binding CloseCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Width="100" HorizontalAlignment="Center"/>
    </StackPanel>
</UserControl>
